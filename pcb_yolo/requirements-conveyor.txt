# Conveyor GUI demo (Pygame + OpenCV). Neural net mode uses `requirements.txt`.
# Install:
#   pip install -r requirements-conveyor.txt
#   pip install -r requirements.txt

pygame>=2.5.0
Pillow>=9.0
opencv-python>=4.8.0
